import { defineCollection } from 'astro:content';

// If you have a blog collection, configure it here
export const collections = {
  blog: defineCollection({
    // Your collection schema here
  }),
};