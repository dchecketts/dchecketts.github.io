---
layout: base.html
permalink: /404.html
eleventyExcludeFromCollections: true
---

<div class="text-center p-8 max-w-lg mx-auto">
  <h1 class="text-6xl font-bold text-blue-600 mb-4">404</h1>
  <h2 class="text-3xl font-semibold mb-6">Page Not Found</h2>
  <p class="text-gray-700 mb-8">
    Sorry, the page you're looking for doesn't exist or has been moved.
  </p>
  <a href="/" class="bg-blue-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors">
    Go Back Home
  </a>
</div>

  <script>
    (function () {
      // If we're not already on the canonical 404 URL, replace the current
      // history entry so the address bar shows `/404` (no reload).
      try {
        const p = window.location.pathname;
        if (p !== '/404' && p !== '/404.html') {
          history.replaceState({}, '', '/404');
        }
      } catch (e) { /* ignore in older browsers */ }
    })();
  </script>
